<Window x:Class="WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bataille Navale" Height="525" Width="525" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Text="Bataille Navale" FontWeight="Bold" FontSize="24" Margin="10"/>
        
        <!-- Statut du jeu -->
        <TextBlock Name="StatutJeuLabel" Text="" FontWeight="Bold" FontSize="12" Margin="200,10,0,0"/>

        <!-- Grille de jeu -->
        <Border BorderBrush="Black" BorderThickness="1" Grid.Row="1" Margin="10">
            <Grid Name="GrilleDeJeu">
                <!-- Lignes et colonnes -->
                <Grid.RowDefinitions>
                    
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                   
                </Grid.ColumnDefinitions>

               
                



            </Grid>
        </Border>

        <!-- Bouton de validation -->
        <Button Content="Valider" Grid.Row="1" HorizontalAlignment="Right" Margin="15" VerticalAlignment="Bottom" Width="75"/>
    </Grid>
</Window>